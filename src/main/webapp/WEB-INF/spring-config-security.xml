<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security" 
    xmlns:beans="http://www.springframework.org/schema/beans" 
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
    xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
        http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-3.2.xsd">
    <!-- HTTP security configurations -->
    <http auto-config="false" use-expressions="true">
        <form-login login-page="LOGIN URI" authentication-failure-url="URI WHERE TO REDIRECT TO IN CASE OF FAILURE" username-parameter="USER INPUT NAME IN FORM" password-parameter="PASSWORD INPUT NAME IN FORM"/>
        <!-- Configure these elements to secure URIs in your application -->
        <intercept-url pattern="LOGIN URI/**" access="permitAll" />
	    <session-management session-fixation-protection="migrateSession" session-authentication-error-url="URI WHERE TO REDIRECT TO IN CASE OF FAILURE"> 
	        <concurrency-control max-sessions="1" error-if-maximum-exceeded="true" expired-url="LOGOUT URI" session-registry-alias="sessionRegistry"/>
	    </session-management>
    </http>
    <!-- Configure Authentication mechanism -->
	
    <beans:bean id="customAuthenticationProvider" class="CLASS WHERE THE AUTHENTICATION PROCESS IS TAKING PLACE"/>
    <authentication-manager alias="authenticationManager">
         <authentication-provider ref="customAuthenticationProvider"/>
    </authentication-manager>
</beans:beans>